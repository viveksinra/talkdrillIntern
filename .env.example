# Backend API origin (no trailing slash). All calls go to <this>/api/v1/...
# Local backend dev server:
NEXT_PUBLIC_API_BASE_URL=http://localhost:2040
# Production:
# NEXT_PUBLIC_API_BASE_URL=https://api.talkdrill.com
